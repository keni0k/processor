Процессор с ассемблером на MIPS протоколе.

MIPS: 
    4 бита для обозначения операции (op);
    5 бит для указания адреса записи (destAddr); 
    5 бит для первого аргумента (firstAddr);
    10 для второго (secondAddr);